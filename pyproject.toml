[project]
name = "pdf-combine"
version = "0.1.0"
description = "A local Windows-friendly PDF combiner"
requires-python = ">=3.11"
dependencies = [
    "Flask>=3.0,<4.0",
    "gunicorn>=23.0,<24.0",
    "Pillow>=10.0,<12.0",
    "pypdf>=5.0,<7.0",
]

[tool.uv]
package = false
